# Python requirements
yt-dlp

# Note: You must manually install the FFmpeg binary and add it to your system's PATH.
# FFmpeg installation guide:
# - Windows: Download from https://ffmpeg.org/download.html and add the 'bin' folder to PATH.
# - Linux: Use `sudo apt install ffmpeg` (Debian/Ubuntu) or equivalent for your distribution.
# - macOS: Use `brew install ffmpeg`.
